/**
 * Sample test — verifies Vitest setup works.
 * Replace with actual component tests.
 */
import { describe, it, expect } from 'vitest';

describe('Salfanet NMS', () => {
  it('should have Vitest configured', () => {
    expect(true).toBe(true);
  });

  it('can run basic math', () => {
    expect(1 + 1).toBe(2);
  });
});
