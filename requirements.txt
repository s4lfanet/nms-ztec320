# === Core ===
Flask>=3.0
Flask-Login>=0.6
Flask-SQLAlchemy>=3.1
Flask-Migrate>=4.0
SQLAlchemy>=2.0
Werkzeug>=3.0
Jinja2>=3.1

# === Security ===
cryptography>=42.0

# === Timezone data (zoneinfo) ===
# Most Linux servers get IANA tzdata from the OS, so this is a harmless no-op
# there — but without it, ZoneInfo() raises ZoneInfoNotFoundError on Windows
# and on minimal containers (e.g. Alpine/musl) with no OS tzdata, breaking
# every timestamp alerts.py formats.
tzdata>=2024.1

# === Network ===
pysnmp>=7.1
paramiko>=3.0,<4.0

# === Async / WebSocket ===
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
httpx>=0.27.0

# === Caching ===
redis>=5.0

# === Task Queue (optional) ===
rq>=1.16

# === Monitoring ===
prometheus-client>=0.20.0

# === Testing ===
pytest>=8.0
